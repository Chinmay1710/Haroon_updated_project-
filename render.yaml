services:
  - type: web
    name: haroon-tailor
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python run_web.py
    envVars:
      - key: PORT
        value: 10000
      - key: PYTHON_VERSION
        value: 3.11.0
